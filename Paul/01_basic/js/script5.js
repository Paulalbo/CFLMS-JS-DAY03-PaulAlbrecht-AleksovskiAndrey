
calculator ("9", "9", "9")

function calculator (width, height, depth) {
	var area = width * height
	var volume = width * height * depth

	document.write("The area of the box is: <strong>" + area + "</strong>" + "<br>");
	document.write("The volume of the box is: <strong>" + volume + "</strong>");
}
